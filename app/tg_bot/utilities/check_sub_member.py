#
#
#
# async def check_sub_member(user_id):
#     for ID in CHANNEL_ID:
#         user = await bot.get_chat_member(chat_id=ID, user_id=user_id)
#         if user.status in ['member', 'administrator', 'creator']:
#             continue
#         else:
#             return False
#     return True
